###
A.yaml and G.yaml are the yaml files of the two Matrixes 'A' and 'G' in the Joint_Bayesian model.

###
frontal_face.xml and profile_face.xml are the xml files of the two Face Detector model used by our CascadeClassifier face detector.

###
DeepID2_deploy.prototxt is the definition file of our DeepID2 network.
DeepID2_deploy.caffemodel is the parameter file of our DeepID2 network pretrained.

###
svm_model.yaml is the model file of our SVM classifier.

###
image_mean.binaryproto is the mean of our images.

### 
libdeepid2.yaml is the config file of our DeepID2 library.
